# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

All project guidance lives in [AGENT.md](AGENT.md): build/flash/test commands, the
host test suites, the SDL2 UI emulator, code conventions (C++17 no-exceptions, no
hot-path allocation, atomic pointer swaps), the component module map, and the
hard design rules ("don'ts"). Read it before working in this repo.
